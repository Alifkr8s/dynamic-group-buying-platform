# Dynamic Group Buying Platform

A Laravel-based group buying platform built with PHP and MySQL.

## Setup
- Clone the repo
- Run `composer install`
- Copy `.env.example` to `.env`
- Run `php artisan key:generate`
- Run `php artisan migrate`